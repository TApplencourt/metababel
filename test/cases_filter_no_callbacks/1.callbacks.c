#include <assert.h>
#include <metababel/metababel.h>
#include <stdio.h>

void btx_register_usr_callbacks(void *btx_handle) {
  // Even when we do not have callbacks to register, we need to defined this
  // function.
}
